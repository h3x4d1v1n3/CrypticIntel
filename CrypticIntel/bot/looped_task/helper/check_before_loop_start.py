# # need to update
# def check_before_loop_task(self):
#     if (len(self.add_by) == 2):
#         if (self.add_by[0] == 'marketcap'):
#             no_of_coins = self.add_by[1]
#             params = {
#                 'start':'1',
#                 'limit': no_of_coins
#             }
#             coins = self.coin_market_cap.get_coins(params)
#             whitelist_coins = []
#             for coin in coins:
#                 self.channel_config[channel_id]['funds'][coin+'USDT'] = 0
                
